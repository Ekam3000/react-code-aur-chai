

function App() {
  // In react project using create-react-app.. the component
  // file type doesn't necessary to be of type .jsx but can
  // also end with .jsx so its not a trict rule.
  //but component name should be initialized with Capital 
  // letter

  return (
   <h1>Hello react with chai</h1>
  );
}

export default App;
